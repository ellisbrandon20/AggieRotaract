<!DOCTYPE html>
<html>
  <head>
    <title>AggieRotaract</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
    
  </head>

  <body>
    
    
    <!-- ****************************************************************-->
    <!-- ************** UNCOMMENT THIS FOR PRODUCTION *******************-->
    <!-- ****************************************************************-->
     if user tries to access pages of the website without logging in they are redirected to the login page 
    <% if session[:user_uin].nil? or session[:admin].nil? %>
      <script type="text/javascript">
          window.location.href="/"  // redirect to root path if user is not logged in
      </script>
    < end %>


   <!-- Nav Bar -->
    <nav class="navbar navbar-toggleable-md navbar-light bg-faded navbar-fixed-top">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- Image or text to get back to "home page" -->
      <a class="navbar-brand" href="/dashboard/index">Aggie Rotaract</a>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="/dashboard/index">Dashboard</a>
            </li>
            <li class="nav-item active">
              <a class="nav-link" href="/events" id="upcoming-events">Upcoming Events</a>
            </li>
            <li class="nav-item active">
              <a class="nav-link" href="#">Points</a>
            </li>
            <!--NOTE: This would have to only be displayed if admin is logged in, ORRRR do we just put this pages purpose in the "points"
                      page, that would make it easier in developing this page and keeping it simple
            <li class="nav-item">
              <a class="nav-link" href="#">Approve Points????</a>
            </li>
            -->
          </ul>
          
          <ul class="nav navbar-nav navbar-right">
          <ul class="nav pull-right nav-item-log-out">
            <%= form_tag :method => :post, :controller => 'user', :action => 'logout' do %>
              <button class="btn btn-default" id="logout_id">Log Out</button>
            <% end %>
          </ul>
          
        </div>
    </nav>
    
    <!--Views are placed here-->
    <div class="body-content">
      <%= yield %>
    </div>
  </body>
</html>
