<style>
  .center {
          margin: auto;
          width: 40%;
          padding: 10px;
        }
</style>

<div>
  <% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %> fade in center" style="">
      <a href="#" data-dismiss="alert" class="close">×</a>
      <%= value %>
    </div>
  <% end %>
</div>